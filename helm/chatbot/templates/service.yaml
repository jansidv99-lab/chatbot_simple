apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-chatbot
spec:
  type: {{ .Values.service.type }}
  selector:
    app: chatbot
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8501
